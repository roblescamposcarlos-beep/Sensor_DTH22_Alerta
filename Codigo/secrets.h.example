// Copia este archivo como "secrets.h" (mismo folder) y llena tus datos reales.
// secrets.h NO se sube al repo (está en .gitignore).

#define WIFI_SSID     "tu_red_2.4GHz"
#define WIFI_PASSWORD "tu_password"
#define WEBHOOK_PATH  "/tu-path-secreto-de-make"
